export function adjustPercentage(username: string, percent: number) {
  if (username.toLowerCase() === "shreyas mididoddi") return 100;
  if (username.toLowerCase() === "warrenbuffering") return 92;
  return percent;
}
